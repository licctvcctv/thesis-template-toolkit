<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heath.community.mapper.MedicineMapper">

    <resultMap id="BaseResultMap" type="com.heath.community.entity.Medicine">
        <id column="id" property="id"/>
        <result column="medicine_code" property="medicineCode"/>
        <result column="medicine_name" property="medicineName"/>
        <result column="specification" property="specification"/>
        <result column="unit" property="unit"/>
        <result column="manufacturer" property="manufacturer"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findPage" resultMap="BaseResultMap">
        SELECT * FROM medicine
        <where>
            <if test="medicineName != null and medicineName != ''">
                AND medicine_name LIKE CONCAT('%', #{medicineName}, '%')
            </if>
            <if test="medicineCode != null and medicineCode != ''">
                AND medicine_code LIKE CONCAT('%', #{medicineCode}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="Long">
        SELECT COUNT(*) FROM medicine
        <where>
            <if test="medicineName != null and medicineName != ''">
                AND medicine_name LIKE CONCAT('%', #{medicineName}, '%')
            </if>
            <if test="medicineCode != null and medicineCode != ''">
                AND medicine_code LIKE CONCAT('%', #{medicineCode}, '%')
            </if>
        </where>
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM medicine WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="BaseResultMap">
        SELECT * FROM medicine WHERE medicine_code = #{code}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO medicine (medicine_code, medicine_name, specification, unit, manufacturer, price, stock, description, status)
        VALUES (#{medicineCode}, #{medicineName}, #{specification}, #{unit}, #{manufacturer}, #{price}, #{stock}, #{description}, #{status})
    </insert>

    <update id="update">
        UPDATE medicine
        <set>
            <if test="medicineCode != null">medicine_code = #{medicineCode},</if>
            <if test="medicineName != null">medicine_name = #{medicineName},</if>
            <if test="specification != null">specification = #{specification},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="manufacturer != null">manufacturer = #{manufacturer},</if>
            <if test="price != null">price = #{price},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM medicine WHERE id = #{id}
    </delete>

</mapper>
