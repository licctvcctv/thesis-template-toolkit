# Design Pattern Chain Validation Case

This directory contains the runnable Java case used by the course paper.

## Run

```bash
javac -encoding UTF-8 -d out src/com/example/validation/*.java
java -cp out com.example.validation.ExperimentRunner
```

The paper copies the source code directly into the DOCX instead of using screenshots.
