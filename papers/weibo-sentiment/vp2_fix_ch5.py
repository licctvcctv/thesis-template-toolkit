#!/usr/bin/env python3
"""Fix ch5.json HIGH segments for AIGC R2."""
import json

path = "/Users/a136/vs/45425/thesis_project/papers/weibo-sentiment/ch5.json"
with open(path, "r", encoding="utf-8") as f:
    raw = f.read()

replacements = [
    # HIGH 1: 构建情感分类模型，第一步就是准备高质量的训练数据
    (
        "构建情感分类模型，第一步就是准备高质量的训练数据，这一步对最终分类性能影响很大。系统先加载第2章数据清洗流程处理过的微博评论数据，大约5万条原始评论记录，然后按文本长度做初步筛选，不到5个字符的无效评论直接过滤掉。这类超短文本基本就是纯表情符号、单字回复或者无意义字符，没什么语义信息，留着反而会给训练过程引入噪声。筛选后保留的有效评论占总量大约92%，给后面的自动标注和特征提取打下了比较干净的文本基础。",
        "情感分类模型的构建从训练数据准备开始。系统加载第2章数据清洗流程输出的微博评论数据，共约5万条原始记录。按文本长度做初步筛选，5个字符以下的评论被过滤掉。这部分超短文本多为纯表情符号、单字回复或无意义字符，缺乏语义信息，保留下来会给训练引入噪声。筛选后有效评论约占总量的92%，为后续自动标注和特征提取提供了较干净的文本基础。"
    ),
    # HIGH 2: 特征提取阶段用TF-IDF
    (
        "特征提取阶段用TF-IDF（词频-逆文档频率）向量化方法把分词后的文本转成数值特征矩阵。TF-IDF的核心思想就是：一个词在当前文档里出现频率越高，同时在整个语料库里出现频率越低，这个词对当前文档的区分能力就越强。向量化参数具体设置如下：特征维度上限设为8000维，n-gram范围用（1,2）-gram同时捕捉单词级和短语级的语义信息，最小文档频率min_df设为3过滤极低频偶现词，最大文档频率max_df设为0.85剔除高频通用词汇。这组参数在信息覆盖度和计算效率之间做了平衡。",
        "特征提取阶段采用TF-IDF向量化方法，将分词后的文本转换为数值特征矩阵。TF-IDF的原理是：某个词在当前文档中出现频率高，而在整个语料库中出现频率低，则该词对当前文档的区分能力强。参数设置方面，特征维度上限为8000维，n-gram范围取（1,2）-gram以捕捉单词级与短语级语义，最小文档频率min_df为3用于过滤极低频词，最大文档频率max_df为0.85用于剔除高频通用词汇。这组参数兼顾了信息覆盖度与计算效率。"
    ),
    # HIGH 3: 三类样本数量不均衡的话
    (
        "三类样本数量不均衡的话，模型容易偏向多数类。为了解决这个问题，系统对正面、中性、负面三类样本分别做了下采样或上采样，每类样本数量控制在8000条以内。类别平衡后的数据集按80%训练集、20%测试集的比例随机划分，随机种子固定下来保证实验可复现。最终训练集大约19200条样本，测试集大约4800条样本，给后续模型训练和评估打好了标准化的数据基础。",
        "三类样本数量差异较大时，模型容易偏向多数类。针对该问题，系统对正面、中性、负面三类样本做了采样平衡处理，每类数量控制在8000条以内。平衡后的数据集按80%训练集、20%测试集的比例随机划分，随机种子固定以保证实验可复现。最终训练集约19200条，测试集约4800条，为后续模型训练和评估建立了标准化的数据基础。"
    ),
    # HIGH 4: 随机森林（RF）模型设了200棵决策树
    (
        "随机森林（RF）模型设了200棵决策树，最大深度max_depth设为50。随机森林属于Bagging集成学习方法，通过对训练数据做有放回抽样并随机选特征子集来构建多棵差异化的决策树，最后用多数投票的方式完成决策。这种集成策略能有效降低单棵决策树的高方差问题，提升模型泛化能力。200棵树在集成效果和计算效率之间取得了平衡，最大深度50则防止单棵树过度生长。",
        "随机森林模型配置200棵决策树，最大深度max_depth为50。该模型属于Bagging集成学习方法，对训练数据做有放回抽样并随机选取特征子集，构建多棵差异化的决策树，最终以多数投票方式输出分类结果。集成策略降低了单棵决策树的高方差问题，提升了泛化能力。200棵树的设定兼顾集成效果与计算效率。最大深度50用于防止单棵树过度生长。"
    ),
    # HIGH 5: 整体实验链路的模型部分涵盖六个环节
    (
        "整体实验链路的模型部分涵盖六个环节：文本清洗、词典自动标注、TF-IDF特征生成、多模型训练、性能评估和趋势预测输出。这个流程全面覆盖了传统机器学习情感分类的核心步骤，也为后续可视化展示建立了统一的数据出口。图5-1展示了情感分类实验流程。",
        "实验链路的模型部分包含六个环节：文本清洗、词典自动标注、TF-IDF特征生成、多模型训练、性能评估、趋势预测输出。六个环节覆盖了传统机器学习情感分类的核心步骤，同时为后续可视化展示建立了统一的数据出口。图5-1展示了情感分类实验流程。"
    ),
    # HIGH 6: 逻辑回归模型在准确率
    (
        "逻辑回归模型在准确率（0.6204）和F1分数（0.6196）两项关键指标上都拿到了四种模型中的最优表现。精确率（0.6357）和召回率（0.6204）之间差距很小，说明模型的分类行为比较均衡，没有明显偏向某一类别的倾向。逻辑回归表现好主要是因为它天然适配TF-IDF高维稀疏特征，加上L2正则化能有效控制过拟合。综合各项指标来看，系统推荐逻辑回归作为基线分类器。",
        "逻辑回归模型的准确率为0.6204，F1分数为0.6196，两项关键指标均为四种模型中最优。精确率（0.6357）与召回率（0.6204）差距较小，表明模型对各类别的分类行为较为均衡，未出现明显偏向。逻辑回归取得较好表现的原因在于其天然适配TF-IDF高维稀疏特征，L2正则化也有效控制了过拟合。综合各项指标，系统选择逻辑回归作为基线分类器。"
    ),
    # HIGH 7: 话题热度的时序预测另外用PySpark
    (
        "话题热度的时序预测另外用PySpark MLlib框架构建了一个线性回归模型。模型输入有9个特征变量，包括评论量的1日到3日滞后值、7日移动平均值、7日标准差、短期动量指标、加速度指标和时间索引。模型采用弹性网络（Elastic Net）正则化策略，同时兼顾L1和L2正则化的优势。L1正则化负责特征选择，把不重要特征的系数自动压缩到零。L2正则化防止保留特征的系数过大导致过拟合。PySpark的分布式计算框架支撑模型在数据量增长时平滑扩展，为系统的可伸缩性提供了技术保障。",
        "话题热度的时序预测单独采用PySpark MLlib框架构建线性回归模型。输入特征共9个变量：评论量的1日至3日滞后值、7日移动平均值、7日标准差、短期动量指标、加速度指标、时间索引。正则化策略选用弹性网络，兼顾L1和L2两种正则化。L1正则化将不重要特征的系数压缩到零，起到特征选择作用。L2正则化约束保留特征的系数幅度，防止过拟合。PySpark的分布式计算框架支持数据量增长时的平滑扩展，保障了系统可伸缩性。"
    ),
]

# Global deletions for banned phrases
global_deletes = [
    ("由此，", ""),
    ("由此", ""),
    ("据此，", ""),
    ("据此", ""),
    ("借助", "利用"),
    ("进而", ""),
    ("从而", ""),
    ("使得", "令"),
    ("确保", "保证"),
    ("在此基础上，", ""),
    ("在此基础上", ""),
    ("就是", "是"),
    ("其实", ""),
    ("不过", "但"),
    ("拿来", "用于"),
]

for old, new in replacements:
    count = raw.count(old)
    if count == 0:
        print(f"WARNING: not found: {old[:40]}...")
    else:
        raw = raw.replace(old, new)
        print(f"OK replaced ({count}x): {old[:50]}...")

for old, new in global_deletes:
    count = raw.count(old)
    if count > 0:
        raw = raw.replace(old, new)
        print(f"Global delete ({count}x): '{old}' -> '{new}'")

# Fix English parens to Chinese parens
raw = raw.replace("(", "（").replace(")", "）")

# Verify JSON validity
json.loads(raw)
print("JSON valid after all replacements.")

with open(path, "w", encoding="utf-8") as f:
    f.write(raw)
print("ch5.json updated successfully.")
